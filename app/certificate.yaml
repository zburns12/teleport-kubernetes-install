apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nginx-site
  namespace: nginx
spec:
  secretName: nginx-tls
  dnsNames:
  - nginx.local
  duration: 720h     
  renewBefore: 240h   
  issuerRef:
    name: lab-ca
    kind: ClusterIssuer
